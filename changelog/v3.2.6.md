# Hotkey Manager v3.2.6

## Changes

- Fixed hotkeys shutting down with an "N hotkeys have been received" warning when a key was held down for a while (such as holding the Copilot key): the built-in runaway-hotkey guard is now set high enough that normal held keys no longer trip it.
