# Hotkey Manager v3.2.7

## Changes

- Hotkeys becoming slow or unresponsive after the app had been sitting in the system tray: the app now continuously re-asserts that it and its hotkey process stay exempt from Windows Efficiency Mode throttling, instead of only doing so once at startup (which Windows was undoing when the app dropped to the tray).
