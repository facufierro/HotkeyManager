# Hotkey Manager v3.3.1

## Changes

- Fixed launch-triggered scripts sometimes being skipped when their target app started within the first three seconds after MacroToolbox.
- Removed the manual script play button; scripts now run only from their configured hotkey or app-launch trigger.
- Fixed key recording so number-pad digits remain distinct from the matching top-row number keys.
