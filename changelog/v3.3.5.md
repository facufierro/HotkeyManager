# MacroToolbox v3.3.5

## Changes

- The main window now opens at launch when an update is available, overriding "Open to tray" so the update banner is immediately visible.
- Hold-to-repeat hotkeys no longer stay active after a trigger release is missed while a different key is being sent.
